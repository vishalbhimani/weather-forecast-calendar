import React from "react";
import { Container } from "@mui/material";
import Calendar from "./components/Calendar";

const App: React.FC = () => {
  return (
    <Container>
      <Calendar />
    </Container>
  );
};

export default App;
